services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - workspace-data:/workspaces/project
      - .:/workspaces/project/.devcontainer # sync this with the host.
    command: sleep infinity

volumes:
  workspace-data: