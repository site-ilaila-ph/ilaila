model FoodImage {
  id          String @id @db.Uuid
  position    Int
  foodId      String  @db.Uuid
  description String
  url         String
  food        Food @relation(fields: [foodId], references: [id])

  @@map("food_images")
  @@schema("app")
}