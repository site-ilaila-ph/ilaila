Note: Not to be commited version control (temporary)

- [x] Convert all database handling into typeorm style instead of prisma style.
- [x] Use concrete types for app constructs instead of using `unknown`, `any`, or `never`.
