export default function Page() {
    /* 
        this is only a stub so next typegen can generate route "/".
        this page is a virtual page that resolves via the proxy.
    */
    throw new Error("Not implemented.");
}